from setuptools import setup

setup(name='probability_distritbution',
      version='1.0',
      description='Gaussian distributions , Binomial Distribution',
      packages=['probability_distritbution'],
      zip_safe=False)
